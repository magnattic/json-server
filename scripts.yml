allow:
  - net
  - read

scripts:
  start:
    cmd: deno run --unstable server.ts ./example/db.json
  start-ts:
    cmd: deno run --unstable server.ts ./example/db.ts
  test:
    cmd: deno test --unstable
